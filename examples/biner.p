#include <console>

main() {
        var
            int;
        int = 10;
        printf("decimal: %d biner: %b", int, int);
        int = 16;
        printf("decimal: %d biner: %b", int, int);
        int = 255;
        printf("decimal: %d biner: %b", int, int);
        int = 21;
        printf("decimal: %d biner: %b", int, int);
       }